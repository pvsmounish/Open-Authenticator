import React, { Fragment } from 'react';
import {
    Text,
} from 'react-native';

const App = () => {
    return (
        <Fragment>
            <Text>Open Authenticator</Text>
        </Fragment>
    );
};

export default App;
