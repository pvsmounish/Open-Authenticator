import React, { Component } from 'react';
import {
    Text,
    View,
    StyleSheet,
} from 'react-native';

class AddServiceScreen extends Component {

    render() {
        return (
            <>
                <Text>Add Service Screen</Text>
            </>
        );
    }
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
});

export default AddServiceScreen;
