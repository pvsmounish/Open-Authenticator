import React from 'react';

import { OpenAuthenticatorNavigation } from './navigator';

const OpenAuthenticatorApp = () => {

	return (
		<OpenAuthenticatorNavigation />
	);
}

export default OpenAuthenticatorApp;